// arethmetic and math

// + - / * <-- Operators with math

console.log(1 + 1);
console.log(20 / 5);

let total = 20 * 5;
console.log(total);

// Comparison Operators - evaluators

let score = 50;

// Sets if the score is "passing"
let passing = score >= 70;

// < <= => > <> == !=. === (comparing strings)<-- evaluators

let isLooseEquality = 5 == "5";
console.log(isLooseEquality);

let isStrictEquality = 5 === "5";
console.log(isStrictEquality);

// Strict equality checks the data type and the data when comparing

// Logical Operators

// And: &&
// Or: ||

console.log(true && false)

// Not !
// give me the opposite of the resulting value
let isTuesday = false;
console.log(!isTuesday)

// You don't want to make variables with negative names

let isNotColdOutside = true
console.log(!isNotColdOutside === true)